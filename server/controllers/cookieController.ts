// import { Request, Response, NextFunction } from 'express';
import db from '../models/db';

const cookieController = {
  // set cookie
  setCookie: async function (req: Request, res: Response, next: NextFunction) {

  },
  // delete cookie
  deleteCookie: async function (req: Request, res: Response, next: NextFunction) {

  }
};

export default cookieController;
