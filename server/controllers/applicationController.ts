import { Request, Response, NextFunction } from 'express';
import db from '../models/db';

const applicationController = {
  // create application
  createApplication: async function (req: Request, res: Response, next: NextFunction) {

  },

  getApplications: async function (req: Request, res: Response, next: NextFunction) {

  },

  updateApplication: async function (req: Request, res: Response, next: NextFunction) {

  },
};

export default applicationController;